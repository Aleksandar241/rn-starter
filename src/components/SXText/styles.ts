import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  textWrapper: {
    flexDirection: 'row',
    overflow: 'hidden',
  },
  textDefault: {
    width: '100%',
    flexWrap: 'wrap',
  },
});

export default styles;
