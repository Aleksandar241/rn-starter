import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  textWrapper: {
    flexDirection: 'row',
    overflow: 'hidden',
  },
  textDefault: {
    flexWrap: 'wrap',
  },
});

export default styles;
